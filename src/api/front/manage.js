// check if there's an Authorization cookie and that it's valid, if not delete the cookie and redirect back to /login
// get /user (api endpoint that takes the authorization token and returns user config)
// build all relevant UI elements.
// ???